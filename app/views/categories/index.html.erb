<p id="notice"><%= notice %></p>

<h1>Categories</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th> -->
      <!-- <th>User</th> -->
     <!--  <th colspan="3"></th>
    </tr>
  </thead>

  <tbody> -->
    <%# @categories.each do |category| %>
     <!--  <tr>
        <td><%#= category.name %></td>
        <td></td> -->
        <!-- <td><%#= category.user_id %></td>
        <td><%#= link_to 'Show', category %></td>
        <td><%#= link_to 'Edit', edit_category_path(category) %></td>
        <td><%#= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr> -->
    <%# end %>
  <!-- </tbody>
</table> -->

<!-- <br> -->

<h1 align="center"><%= link_to 'New Category', new_category_path, class: "btn btn-primary" %></h1>

<div class="table-responsive">          
  <table class="table datatable table-bordered">
    <thead>
      <tr class="bg-primary">
        <th>Friends</th>
        <th>Action</th>
        <th>Action</th>

      </tr>
    </thead>
    <tbody>
      <% @categories.each do |category| %>
        <tr>
          
        <td><%= category.name %></td>
        <td><%= link_to 'Show', category %></td>
        <td><%= link_to 'Edit', edit_category_path(category) %></td>
        <!-- <td><%#= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
